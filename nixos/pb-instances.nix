# PocketBase instance definitions shared between configuration.nix and caddy.nix
{
  postador = { port = 8090; domain = "api.postador.com.br"; };
  # Add more instances here:
  # app2 = { port = 8091; domain = "api.app2.com.br"; };
}
